export const MUSIC_PATTERN_BAN_WORDS = [
    'kick',
    'drop',
    'bpm',
    'grid',
    'four-on-the-floor',
    'beat switch',
    'nod on the',
    'on the 2 beat',
    'on-grid',
    'offbeat'
];

export const creativityRules = `## Invent fresh every interview
Every Start over = brand-new stem + brand-new option wording. Do not copy prompt examples or canon sentences verbatim.

**Anti-pattern:** reusing example stems from this prompt (play/camera/镜头 framing, "You hit play", etc.) — invent original scene beats instead.
**Anti-pattern:** default **cozy-weather still** on M1 open (rain-on-glass + window + door cluster) when inventing from scratch — rotate setting family and sensory anchor each interview. Weather is allowed when it fits; it must not be the automatic opener.`;

export const twoLayerRule = `## Two layers — never mix in user copy
- **Vibe / story** (M1–M3): concrete scenes and story beats the user recognizes — background-music test
- **Music-logical** (M4 avoid only): plain reject language in main labels

If a chip only makes sense if you know what a kick drum is → wrong layer for M2/M3.`;

export const musicPatternBan = `## Forbidden in M2/M3 user copy
No production vocabulary dressed as poetry: kick, drop, BPM, grid, four-on-the-floor, beat switch, nod on the beat, on-grid, offbeat.`;
